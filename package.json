{
    "name": "ssr-render-puppeteer",
    "private": true,
    "version": "0.0.0",
    "type": "module",
    "scripts": {
        "start": "cd client build && yarn dev",
        "dev": "concurrently \"cd client && yarn preview\" \"cd server && yarn dev:server\"",
        "install:all": "yarn install && cd client && yarn install && cd ../server && yarn install"
    },
    "devDependencies": {
        "concurrently": "8.2.2"
    }
}